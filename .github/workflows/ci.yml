name: ci

on:
  push:
  pull_request:

jobs:
  public-safety:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run public-source safety checks
        shell: pwsh
        run: .\scripts\check-public-safety.ps1
